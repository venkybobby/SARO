fastapi>=0.111.0
uvicorn[standard]>=0.29.0
gunicorn==22.0.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9
pydantic[email]>=2.7.0
argon2-cffi>=23.1.0
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0
httpx>=0.27.0
requests>=2.31.0
PyYAML>=6.0
structlog>=24.1.0
numpy>=1.26.0
scipy>=1.13.0
scikit-learn>=1.4.0
tenacity>=8.3.0
fpdf2>=2.7.0
python-dotenv>=1.0.0
python-multipart>=0.0.9
reportlab>=4.0.0
redis>=5.0.0
fakeredis>=2.23.0
python3-saml>=1.16.0
sendgrid>=6.11.0
anthropic>=0.40.0
cryptography>=42.0.0
weasyprint>=62.0
# boto3 is optional — only needed when AWS_S3_BUCKET env var is set (large-text S3 offload)
# boto3>=1.34.0
# S-002: HuggingFace dataset sampling
datasets>=2.18.0
huggingface-hub>=0.22.0
# SARO Data Framework — offline evaluation batch jobs
# NOTE: saro-data-framework is a local package installed separately in
# the Dockerfile Stage 2 (after COPY . /app) to avoid path resolution
# issues in multi-stage Docker builds. See Dockerfile for the RUN step.
# For local dev: pip install -e ./saro-data-framework
python-dateutil>=2.8.0
