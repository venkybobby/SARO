[pytest]
markers =
    unit: pure-unit tests (no DB, no network)
    integration: integration tests requiring a live Supabase connection
    e2e: end-to-end Playwright browser tests
    security: security scan tests
asyncio_mode = auto
