# SARO Findings Ledger

Every review/incident/audit finding gets an `FND-###` ID here. A finding is **closed only
when a regression test pins it** (listed in `tests/regression/manifest.yaml`).
Workflow: log here → root-cause → fix → write `tests/regression/test_fnd_###_*.py` → update manifest.

| ID | Title | Source | Discovered | Root Cause | Status |
|---|---|---|---|---|---|
| FND-001 | Redis session management broken (legacy audit) | audit | 2026-03 | session state not invalidated server-side | verify-pinned |
| FND-002 | Missing DELETE /auth/logout endpoint (legacy audit) | audit | 2026-03 | endpoint never registered | verify-pinned |
| FND-003 | Hardcoded JWT secret (legacy audit) | audit | 2026-03 | secret committed instead of env var | verify-pinned |
| FND-004 | TraceView: undefined `h` causes ReferenceError on audit-meta fetch | eslint flat-config rollout | 2026-06 | `frontend/src/pages/TraceView.jsx:86` referenced an undeclared `h` instead of an `Authorization` headers object | pinned |
| FND-005 | RiskRegister: undefined `toast` causes ReferenceError on bulk/delete actions | eslint flat-config rollout | 2026-06 | `frontend/src/pages/RiskRegister.jsx` called `toast?.success`/`toast?.error` but `toast` was never destructured from props | pinned |
| FND-006 | scan_reports.tenant_id never backfilled / never set on insert — NULL rows invisible under RLS to all tenants | PR #68 review | 2026-06 | `migrations/002_add_tenant_id_columns.sql` added `scan_reports.tenant_id` (nullable) and backfilled `audits`/`audit_traces` but not `scan_reports`; `models.ScanReport` never declared the column, so every `ScanReport(...)` insert site left it NULL. Under `tenant_isolation_scan_reports` (`tenant_id::text = current_setting(...)`), `NULL = x` is `NULL` not `true`, so affected rows are invisible to every tenant | pinned |
| FND-007 | `risk_detail` navigation silently falls through to Dashboard | SARO_AIInsights_Stories implementation | 2026-06 | `RiskRegister` (and now AIInsights) call `onNavigate("risk_detail", …)` but `PAGE_COMPONENTS` in `frontend/src/App.jsx` never registered the key and never wired RiskDetail's `riskId` prop, so `PAGE_COMPONENTS[activePage] \|\| Dashboard` rendered Dashboard instead | pinned |
| FND-008 | Bad merge left duplicate JSX in RiskForm.jsx and a non-compiling RiskForm.test.jsx, masking 13 tests | SARO_AIInsights_Stories gate run | 2026-06 | A merge of PR #61/#67 kept both old and new versions of lines: duplicate `id="*-error"` divs (one `#ef4444`, one `var(--color-critical)`) in `RiskForm.jsx`, plus duplicate imports and two unterminated blocks in `RiskForm.test.jsx`. The test file failed esbuild transform, so the whole file (13 tests incl. STORY-RISKFORM-001/002 coverage) never ran and the entire frontend suite was red | pinned |
| FND-009 | Insights action authz is a persona denylist, not an allowlist | security-auditor (SARO_AIInsights_Stories) | 2026-06 | `routers/insights.py` blocked only `ai_auditor`; `User.persona_role` is nullable, so NULL/future personas defaulted to write. Fixed (PT-009): `auth.require_write_persona` allowlist applied to insight action + all risks.py write endpoints | pinned |
| FND-010 | LIKE wildcard injection in routers/risks.py risk-id prefix lookup | security-auditor (SARO_AIInsights_Stories) | 2026-06 | `_find_audit_with_meta` passed `R-`-stripped user input into `like(f"{prefix}%")` unescaped — `%`/`_` matched arbitrary same-tenant audits. Fixed (PT-009): ported `_HEX_PREFIX` validation from insights `_safe_prefix()` | pinned |
| FND-016 | FastAPI dependency_overrides leak across tests | pressure-test gap batch (PT-007 added test files, shifting collection order) | 2026-06 | Modules set `app.dependency_overrides` inside tests / autouse fixtures without teardown. A leaked authenticated `get_current_user` + removed `get_db` made `test_sar014`/`test_p2` auth tests fail (or hit the real DB) under certain orderings; the baseline only passed by collection-order luck. Fixed: root conftest autouse fixture snapshots+restores the override map per test | pinned |
| FND-011 | `AuditTrace` ORM model missing `tenant_id` (+ `gate_id` NOT NULL drift) — backend won't start on a fresh Postgres | STORY-CI-001 / local bring-up | 2026-06 | `main.py` lifespan runs `create_all_tables()` before `apply_pending_migrations()`. `models.AuditTrace` never declared `tenant_id`, so create_all built `audit_traces` without it and migration `000`'s `CREATE INDEX … ON audit_traces(tenant_id)` failed at startup (mirror of FND-006 for `ScanReport`). `gate_id` was also `NOT NULL` while migration `015` relaxes it for explain/remediate summary rows. Fix: nullable `tenant_id` FK + nullable `gate_id` on the model. Pure-`psql` chain (AC-3) was unaffected, masking it | pinned |
| FND-012 | `persona_permissions` migration 004 seed fails against the ORM-shaped table on the app-startup path | STORY-CI-001 (flagged out-of-scope there) / local bring-up | 2026-06 | `004_add_persona_permissions.sql` seeds via unconditional `INSERT` assuming the legacy table (SERIAL `id`, `denied_actions`/`trace_mode` cols). On startup `create_all_tables()` builds the newer ORM shape (UUID `id` no default, no `denied_actions`) first, so the seed hit `column "denied_actions" does not exist` then `null value in column "id"`, hard-failing the runner. Fix: guard the legacy seed in a `DO $$` block that runs only when `denied_actions` exists; runtime `seed_persona_permissions()` populates the ORM shape | pinned |
| FND-013 | `AuditTrace(...)` insert sites omit `tenant_id` — new trace rows written with NULL tenant_id | security-auditor (FND-011 review) | 2026-06 | FND-011 added the `audit_traces.tenant_id` column but `_persist_traces()` (`routers/scan.py`) and `scripts/seed_demo.py` construct `AuditTrace(...)` without setting it (unlike the five `ScanReport(...)` sites fixed in FND-006). Fail-closed today (no cross-tenant leak: RLS GUC `app.current_tenant` is never `set_config`-ed at runtime, and trace reads scope via the parent audit's tenant), but it negates the defense-in-depth column the moment RLS is activated. Fix: source `tenant_id` from the parent audit at each `AuditTrace(...)` site — `routers/scan.py` reads it from the locked parent-audit row in `_persist_traces`; `scripts/seed_demo.py` uses `audit_obj.tenant_id`. Pinned by `test_fnd_013_audit_trace_constructors_set_tenant_id.py` (constructor scan mirroring `test_fnd_006`) | pinned |
| FND-014 | Gate-3 LLM judge fed the matched-signal label instead of the sample text | STORY-101 | 2026-06 | `_gate3_risk_classification` passed `flag.signal` (e.g. `"keyword:ssn"`) into the judge instead of the redacted sample text; `_SampleFlag` carried no text. Fix: add a PII-redacted `text` field and pass `flag.text`; fail-safe keeps the flag when text is empty. Pinned by `tests/test_sar012_llm_classification.py::test_gate3_judge_receives_sample_text_not_signal_label` | pinned |
| FND-015 | `POST /api/v1/reports/{audit_id}/iso42001-annex` (the one mutating Reports endpoint) lacks `require_write_access` | security-auditor (STORY-110) | 2026-06 | Gated only by Reports access + an internal persona check, not `require_write_access`. A read-only demo persona reaching a write endpoint is a latent inconsistency. Fix: add `Depends(require_write_access)`. Pre-existing; out of STORY-110 scope | open |
| FND-017 | `GET /api/v1/risk/board-export` PDF lacked a board-persona RBAC guard | Dashboard screen review | 2026-06 | `_require_board_persona` (risk_officer/super_admin) was defined but never wired into `export_board_pdf`, so any authenticated tenant user (incl. read-only `ai_auditor`/`operator`) could pull the formatted board risk pack — while the newer SPEC-FE2 export already gated via `_require_board_access`. Fix: gate the export via the shared `_require_board_access`; tenant scoping was already enforced. Pinned by `test_fnd_017_board_export_rbac.py` | pinned |
| FND-018 | `/risk/board-export` PDF missing the required compliance disclaimer | Dashboard screen review (compliance-guard) | 2026-06 | `docs/COMPLIANCE_CLAIMS_MATRIX.md` mandates the disclaimer on every exported report; the SPEC-FE2 export carried it but the older `export_board_pdf` did not. Fix: shared `_REPORT_DISCLAIMER` constant appended by both exporters. Pinned by `test_fnd_018_report_disclaimer.py` | pinned |
| FND-019 | Board summary emitted hardcoded fake framework-coverage percentages (EVF Tier-3 overclaim) | Dashboard screen review (compliance-guard) | 2026-06 | `get_board_summary` returned `{EU AI Act: 72.0, NIST: 68.0, AIGP: 55.0, ISO 42001: 48.0}` and rendered them into the board PDF as "Coverage %", but per SARO-RISK-001 no framework claim has completed EVF (Tier-3 → no external coverage claim). Fix: framework_coverage now maps each framework to its EVF validation-status string; PDF renders status, not %. Pinned by `test_fnd_019_framework_coverage_evf.py` | pinned |
| FND-020 | `/risk/summary` hardcoded `findings=[]` — finding-derived metrics structurally zero | Dashboard screen review | 2026-06 | `get_risk_summary` called `build_risk_summary(records, findings=[])`, so `top_findings`/`remediation_pct` (and the dashboard's open/critical counts) were always empty/zero, contradicting the hardcoded KPI cards. Fix: `_get_finding_records` pulls real failing traces (tenant-scoped via parent audit_ids) and feeds `build_risk_summary`, which also exposes `open_findings_count`/`critical_findings_count`. Pinned by `test_fnd_020_risk_summary_real_findings.py` | pinned |
| FND-021 | Dashboard duplicate Quick Actions block rendered every action twice | Dashboard screen review | 2026-06 | `Dashboard.jsx` contained the entire Quick Actions `<div>` block pasted verbatim twice (copy-paste). Fix: deleted the duplicate. Pinned by `frontend/src/pages/Dashboard.test.jsx` | pinned |
| FND-022 | Dashboard posture banner labels mismatched their data | Dashboard screen review | 2026-06 | `derivePosture` mapped `openRisks: data.audit_count` and `overdue: data.top_findings.length` under labels "Open Risks"/"Overdue" — neither matched its data, and "Overdue" was structurally always zero. Fix: banner now shows Risk Score (`overall_risk_score`) + Open Risks (`open_findings_count`). Pinned by `frontend/src/pages/Dashboard.test.jsx` | pinned |
| FND-023 | Dashboard KPI delta arrows were fabricated literals, not real trends | Dashboard screen review (compliance-guard) | 2026-06 | Every `delta` in `PERSONA_KPIS` was a hardcoded literal rendered as "↑ +N since last period" — a fabricated measured-change claim in a compliance product, while a real `/risk/whats-changed` 7-day delta endpoint existed unused. Fix: removed the fabricated deltas; added a single `<TrendLine>` backed by `/risk/whats-changed`. Pinned by `frontend/src/pages/Dashboard.test.jsx` | pinned |
| FND-024 | `routers/risk_dashboard.py` handlers are sync `def` running blocking SQLAlchemy on the event loop | Dashboard screen review (api-conventions) | 2026-06 | api-conventions requires async-only handlers, but `get_risk_summary`/`get_vendor_risk`/`get_whats_changed`/`export_board_pdf`/`get_board_summary`/exporters are `def`. A bare `async def` rename is insufficient (internal sync callers + direct-call tests + blocking-I/O-on-loop), so remediation is a scoped story with an architectural decision (async session vs. `run_in_threadpool` vs. documented exemption). Tracked by `specs/stories/STORY-115_async_db_handler_migration.md`; no regression test yet (forward work). | open |
| FND-027 | `GET /api/v1/audit/{id}/trace` + export variants not tenant-scoped — cross-tenant TRACE leak | STORY-TRACE-002 | 2026-06 | `routers/trace_view.py:_get_audit_or_404(db, audit_uuid)` filtered only on `Audit.id` with no tenant predicate, so any authenticated user could read any tenant's full TRACE (reasoning + findings) and signed exports by UUID via the timeline endpoint and its three export variants (`/trace`, `/trace/export`, `/export/json`, `/export/pdf`). The sibling `/api/v1/traces/{id}` path was already scoped. Fix: helper now takes `tenant_id` and filters `Audit.id` + `Audit.tenant_id`, raising a generic 404 ("Audit not found") so foreign-tenant and nonexistent audits are indistinguishable (no existence oracle). Pinned by `test_fnd_027_trace_view_tenant_scope.py`. | pinned |
| FND-028 | AI Auditor / Compliance Lead personas locked out of the TRACE View's own endpoints | STORY-TRACE-003 | 2026-06 | The TRACE View is the `ai_auditor`'s primary screen, but `/api/v1/audits/{id}` and (the trace-inspection use of) `/api/v1/audits` were gated `require_role("super_admin","operator"[,"demo_viewer"])` — the `role` axis — so an `ai_auditor`/`compliance_lead` persona on a non-operator role got 403 (surfaced in-UI as "audit not found"); the timeline endpoint `/api/v1/audit/{id}/trace` had no gate at all. Fix: reusable `auth.require_role_or_persona(roles, personas)` allow-list (mirrors `_require_reports_access`); applied to the timeline + all export variants (`trace_view.py`) and to audit detail + audits list (`scan.py`), admitting `ai_auditor`/`compliance_lead` read-only alongside the legacy roles (list keeps `demo_viewer`). Read-only: mutation routes (`POST /traces/{id}/remediate`, in untouched `traces.py`) stay denied; tenant scoping unchanged. Pinned by `test_fnd_028_trace_audit_read_access.py`. | pinned |
| FND-029 | TRACE View rendered the trace array as an object → failing audits shown as fully passed | STORY-TRACE-001 | 2026-06 | `TraceView.jsx` fetched `/api/v1/traces/{id}` (a `list[AuditTraceOut]` array) and read it as an object (`trace.status`, `trace.risk_score`, `trace.steps[key]`, `trace.steps_completed`), all `undefined` on an array. So the status badge was always PENDING, the risk chip never rendered, and the 6-step strip showed all-green ("done") via the `steps_completed \|\| 6` fallback even for a failing audit — an overclaim on the auditor's primary screen. Fix: repoint to the timeline endpoint `GET /api/v1/audit/{id}/trace`; a unit-tested `normalizeTrace()` helper keys the `steps` array by step; status is `found?.status \|\| "pending"` (no all-green fallback); header binds real `audit_status` + `risk_score` (the latter now surfaced on the timeline). Pinned by `frontend/src/pages/TraceView.test.jsx`. | pinned |
| FND-030 | TRACE View asserted "Hash chain integrity: verified" from an absent field (unbacked overclaim) | STORY-TRACE-004 | 2026-06 | The banner read `trace.hash_chain_valid` — returned by neither endpoint — so on the tamper-evidence screen it either never rendered or would assert a verification the client never performed (ADR-004 anti-overclaiming). Fix: the timeline endpoint computes a server-side `integrity` verdict by recomputing the HMAC-SHA256 signature over the canonical export (`trace_export._build_signed_json`) and comparing to the recorded `export_hash`. A positive "verified" renders ONLY on a real HMAC match (with the specific wording + short hash); no signed export, an un-runnable check, or a mismatch all render a neutral "Integrity not verified" — never a green claim. A mismatch is deliberately NOT asserted as "tampered" because `export_hash` may have been written by the alternate plain-SHA-256 path (`output_audit.py`). Pinned by `test_fnd_030_trace_integrity_verdict.py` (+ banner behavior in `TraceView.test.jsx`). | pinned |
| FND-031 | TRACE View "Recent Traces" read `a.risk_score` but the audits list returns `overall_risk_score` → score chips always blank | STORY-TRACE-007 | 2026-06 | `TraceView.jsx` recent-trace chips read `a.risk_score` while `GET /api/v1/audits` returns `overall_risk_score` (AuditListItemOut), so every chip rendered with no score. (The list was also empty for the `ai_auditor` persona until STORY-TRACE-003 granted access.) Fix: read `a.overall_risk_score ?? a.risk_score` (legacy fallback), scale 0–1 → ×100 once, threshold colors 70/40 matching the rest of the screen; a null score omits the number (no "0"/"NaN"); an empty tenant shows a distinct "no recent traces" state separate from an access/transient failure. Pinned by `frontend/src/pages/TraceView.test.jsx`. | pinned |
| FND-032 | TRACE View collapsed 403 / 404 / network failures into one "audit not found" message | STORY-TRACE-010 | 2026-06 | On any non-OK response `TraceView.jsx` threw `"${status} — audit not found"`, so a 403 (no access), a real 404 (missing), and a network/5xx failure all read identically as "audit not found" — opaque to the auditor (EU AI Act Art. 13 transparency). Fix: differentiated error states — 403 → access-oriented message, 404 → not-found, network/5xx → transient message with a Retry that re-fetches only the trace; real `Skeleton` placeholders during load (not bare "Loading…"); loading/error/loaded states are mutually exclusive; the zero-steps success keeps its distinct empty state. Pinned by `frontend/src/pages/TraceView.test.jsx`. | pinned |
| FND-033 | TRACE export endpoints read nonexistent `report.risk_score`/`report.confidence` → null JSON scores + PDF 500 | reviewer (STORY-TRACE-006) | 2026-06 | `routers/trace_view.py` `export_trace_json` used `getattr(report, "risk_score", None)`/`getattr(report, "confidence", None)` (silently always null) and `export_trace_pdf` read `report.risk_score`/`report.confidence` — `ScanReport` has `overall_risk_score`/`confidence_score`, not those. With reportlab installed (prod), the PDF path raised AttributeError → 500; the `try` caught only `ImportError`. STORY-TRACE-006 wired the Export buttons to these endpoints, exposing it. Fix: read `report.overall_risk_score`/`report.confidence_score` in both. Pinned by `test_fnd_033_trace_export_report_fields.py`. | pinned |
| FND-025 | `GET /api/v1/audits` excluded the `compliance_lead` persona (its own landing page) | CHUB-002 (Compliance Hub screen review) | 2026-06 | `list_audits` (`routers/scan.py`) was gated `require_role("super_admin","operator","demo_viewer")`. `compliance_lead` is a `persona_role`, not a system `role`, so a real compliance lead got 403; `ComplianceHub.jsx` swallowed it (`.catch(() => {})`) and always showed "No audits yet." Fix: new `auth.require_role_or_persona` grants access by role OR persona; tenant scoping unchanged; frontend surfaces a visible error instead of an empty state. **Authz scope reconciled (FND-035, lead decision 2026-06-24):** canonical persona set is `{ai_auditor, compliance_lead}` (the broader CHUB `risk_officer/admin` set is superseded). Pinned by `test_fnd_025_audits_compliance_lead_access.py` | pinned |
| FND-026 | Compliance Hub Recent Audits risk-score column always rendered "—" | CHUB-003 (Compliance Hub screen review) | 2026-06 | `ComplianceHub.jsx` read `a.risk_score`, but `/api/v1/audits` (`AuditListItemOut`) exposes the field as `overall_risk_score`, so the key never matched and `RiskBadge` never rendered. Fix: read `a.overall_risk_score` (legacy `a.risk_score` kept as defensive fallback); null → "—", genuine 0 → green badge. Pinned by `frontend/src/pages/ComplianceHub.test.jsx` | pinned |
| FND-036 | Bad merge left `main` non-importable — `auth.py` + `routers/scan.py` SyntaxErrors + dropped manifest `status` | post-merge verification (CHUB/TRACE/Epic-14 PRs landed together) | 2026-06 | Merging the in-flight PRs spliced two copies of `auth.require_role_or_persona` (unclosed `raise`), duplicated the `dependencies=` kwarg + a `from auth import` line on `GET /api/v1/audits`, and dropped `status` from the FND-033 manifest entry → `import auth` raised SyntaxError, the backend wouldn't start, and the whole suite failed collection. Fix: collapsed to the security-hardened `require_role_or_persona` (generic 403, no identifier echo); removed the duplicate kwarg/import (route uses the reconciled `_require_audits_list_read`); restored the FND-033 manifest fields. Pinned by `test_fnd_036_repair_bad_merge_import.py`. | pinned |

| FND-039 | RPV-001 `verify_chain` trusted stored `content_hash` — manifest tampering undetectable | reviewer + security-auditor (STORY-RPV-001) | 2026-07 | `services/rule_pack_snapshot_service.verify_chain` recomputed only `record_hash` (from the stored `content_hash`), never re-deriving `content_hash` from `snapshot_manifest`. An out-of-band DB edit to the manifest — the per-row rule hashes RPV-002 pins evidence to — that left `content_hash` stale still returned `valid:true`, defeating AC-5 under the story's own out-of-band-edit threat model (the DB trigger blocks this on real Postgres, so it was a defense-in-depth hole, not a live exploit). Fix: `verify_chain` now recomputes `_content_hash(snapshot_manifest)` and flags a break (`content_hash_mismatch`) if it diverges from the stored value. Pinned by `test_fnd_039_rpv_snapshot_integrity.py`. | pinned |

| FND-040 | RPV-002 `reproduce_criteria` returned `snapshot_content` outside the hash chain — tamperable while reporting valid | reviewer (STORY-RPV-002 B2) | 2026-07 | `snapshot_content` (the frozen rows an examiner reads) was stored on the snapshot but never covered by the `content_hash`/manifest chain and never cross-checked at read time. An out-of-band edit to `snapshot_content` was returned by `reproduce_criteria` while `verify_chain` still reported valid — the FND-039 bug class one layer deeper. Fix: `reproduce_criteria` re-hashes each stored row (`_hash_row_payload`) and compares to the chain-covered manifest; a mismatch sets `content_integrity=False`, `reproduction_available=False`, and an incident limitation. Pinned by `test_fnd_040_rpv_reproduction_integrity.py`. | pinned |
| FND-041 | RPV-002 pin in `_PAYLOAD_FIELDS` would false-fail every pre-RPV-002 evidence chain | reviewer (STORY-RPV-002 B1) | 2026-07 | Adding `rule_pack_version_id`/`rule_pack_content_hash` to the unconditionally-hashed payload changed the canonical JSON for records whose stored `content_hash` predated those keys, so `verify_chain` recomputed a different hash and raised a false "content_hash mismatch (payload mutated)" across all historical records — violating AC-4. Fix: pin fields moved to `_PIN_FIELDS`, folded into the payload ONLY when non-NULL, so a NULL-pin (legacy or PERMISSIVE-unpinned) record serializes byte-for-byte as before. Pinned by `test_fnd_041_evidence_pin_backcompat.py`. | pinned |

| FND-042 | Scoring engine served DRAFT/RETIRED obligation text as authoritative in AppliedRuleOut + TRACE | security-auditor (STORY-CHUB-011 Blocker) | 2026-07 | `engine.py` built `_eu_rules`/`_gov_rules` from `db.query(EUAIActRule/GovernanceRule).all()` with no validation_status filter, so `_lookup_obligations` attached DRAFT_UNVALIDATED / RETIRED obligation text (e.g. the retired US EO 14110 rows and drafted EU Arts 9-17) to Gate-4 applied rules returned to callers and written to the TRACE timeline — the matrix hid these rows (CHUB-011) but the scan path still presented them as authoritative (AC-1 "TRACE rule references"; AC-5 "draft rules must not influence attestations"). Fix: both loads filter `validation_status IN rule_visibility.default_visible_statuses()` (fail-closed, honors saro_show_legacy_rules). Pinned by `test_fnd_042_engine_excludes_draft_rules.py`. | pinned |
| FND-043 | SSO can self-assign the privileged `ai_auditor`/`admin` persona that unlocks the draft toggle | security-auditor (STORY-CHUB-011 Should-fix) | 2026-07 | `routers/sso.py` sets `persona_role` directly from a SAML assertion attribute with no allow-list validation; CHUB-011 newly makes `ai_auditor`/`admin` a data-disclosure boundary for unvalidated regulatory content, so a spoofable/over-permissive IdP attribute could reveal DRAFT/RETIRED rows. Pre-existing trust-boundary issue (already governs write personas), not introduced by CHUB-011. Forward work: validate incoming `persona_role` against the known vocabulary in sso.py and confirm the IdP attribute is not user-editable. No regression test yet. | open |

| FND-044 | Hardcoded default HMAC secret on the evidence-export path | security-auditor (STORY-TEN-001, pre-existing) | 2026-07 | `routers/trace_export.py` uses `os.environ.get("EXPORT_HMAC_SECRET", "saro-default-hmac-secret-change-in-prod")` — if the env var is unset in prod, TRACE evidence exports are signed with a public default secret, so their HMAC integrity signature is forgeable. Outside the TEN-001 diff (flagged because a CISO scrutinizes the evidence-export path). Forward work: require EXPORT_HMAC_SECRET (fail fast if unset in non-test env); no default in prod. No regression test yet. | open |

| FND-045 | Usage-meter dimension VALUES were unbounded — PHI-free guarantee was key-only | security-auditor (STORY-MTR-001) | 2026-07 | `services/metering_service._validate` bounded dimension KEYS to a closed vocab but performed zero validation on VALUES, which are stored verbatim in the `dimensions` JSONB. A future caller could smuggle raw payload/PII via a value (e.g. `adapter_id="<raw output>"`), structurally undermining AC-1 ("PHI-free by construction, not by redaction"). Fix: `_validate` now requires bounded scalars, a hard 64-char length cap, and a closed per-key value allow-list where defined (`outcome` ∈ {GO, GO_WITH_CONDITIONS, NO_GO, unknown}). Pinned by `test_fnd_045_metering_phi_free_values.py`. | pinned |

| FND-046 | Observation checkpoint watermark_position was unbounded free text — PII could be stored in the coverage evidence (INV-2) | security-auditor (STORY-COV-001) | 2026-07 | `routers/observation_coverage.CheckpointIn` bounded strings with `min_length=1` only (no max/pattern), so a caller could POST a free-text/PII blob as `watermark_position` and it would persist verbatim (≤255 chars on Postgres, unbounded on the SQLite test harness), defeating INV-2 (checkpoints carry positions/timestamps ONLY, never content). Fix: `max_length=255` on all three id/position fields + an opaque-token `pattern` on `watermark_position` so free-text is rejected 422. Pinned by `test_fnd_046_coverage_watermark_bounded.py`. | pinned |

| FND-047 | `record_checkpoint` SELECT-then-INSERT race poisoned the shared session on coalesced concurrent emits | reviewer (STORY-COV-002 Should-fix) | 2026-07 | STORY-COV-002 wires `emit_observation` into the live audit path and deliberately coalesces concurrent audits onto the same `obs:{bucket}` watermark. `record_checkpoint` did an unlocked SELECT-then-INSERT, so two concurrent callers both passed the existence check and raced to INSERT; the loser's UNIQUE collision surfaced at autoflush/commit and — with no `rollback` — left the request-shared session in `PendingRollbackError`, silently poisoning downstream work on that session (e.g. the SAR-008 risk notification after the scan hook). Fix: wrap the whole add→flush→commit in `try/except IntegrityError: db.rollback(); return None`, treating the collision as the idempotent no-op the coalescing design already intends (mirrors `metering_service.safe_increment`). Pinned by `test_fnd_047_checkpoint_race_idempotent.py`. | pinned |
| FND-048 | `saro demo reset` left `AuditEvent`/`Notification` rows behind | reviewer (STORY-410 verify stage) | 2026-07 | `demo_reset` (`cli.py`) originally deleted only Audit/Disposition/ObservationCheckpoint/ObservationGap for the target tenant. Disposition actions write `AuditEvent` rows via the self-audit spine; threshold/drift alerts write `Notification` rows. Neither was cleared, so a rehearsal that included a disposition/acknowledgment step left a stale self-audit/notification trail after reset, undermining AC-4.4 ("reset → re-ingest reproduces a first-run ingest"). Fix: `demo_reset`'s tenant-scoped delete set (`tenant_scoped_models`) now includes `AuditEvent` and `Notification`. Pinned by `test_fnd_048_demo_reset_clears_audit_event_and_notification.py`. | pinned |
| FND-049 | `saro ingest` surfaced a raw traceback on a source-discovery failure | reviewer (STORY-410 verify stage) | 2026-07 | `_run_ingest`'s call to `discover_object_keys(...)` (`cli.py`) was unguarded — a source-level failure (bad bucket, permissions, an unreachable path) propagated as a raw exception instead of AC-1.3's promised clear, operator-facing message. Per-object-key read failures inside the discover-then-parse loop were already handled; the discovery call itself was not. Fix: wrapped in try/except raising `CliError`. Pinned by `test_fnd_049_ingest_discover_failure_is_clear_error.py`. | pinned |
| FND-050 | `submit_audit_sync` silently dropped `metadata` instead of forwarding it to `run_output_audit` | reviewer (STORY-411 verify stage) | 2026-07 | `services/audit_submission.py`'s `submit_audit_sync` has received `metadata` as a parameter since STORY-406 (used to build `ScanReport.report_json["adapter_metadata"]`), but never passed it into `engine_obj.run_output_audit(...)` — only `audit_id`/`raw_output`/`prompt`/`source_model` were forwarded. This kept `run_output_audit` structurally blind to envelope fields (`modelId`/`requestId`) even after the engine gained the capability to evaluate them for STORY-411, and would silently block any future envelope-only evaluation the same way. Fix: forward `metadata=metadata` into `run_output_audit`. Pinned by `test_fnd_050_submit_audit_sync_forwards_metadata.py`. | pinned |
| FND-051 | `GET /api/v1/evf/qco/expiry-alerts` shadowed by evf_sprint2's generic `/qco/{qco_id}` route | build (STORY-412 demo-tab endpoint census) | 2026-07 | `evf.py`, `evf_sprint2.py` and `evf_sprint3.py` all share the `/api/v1/evf` prefix. `main.py` included `evf_sprint2_router` (generic `GET /qco/{qco_id}`) before `evf_sprint3_router` (static `GET /qco/expiry-alerts`); FastAPI matches routes in registration order, so every call to `expiry-alerts` was parsed as `qco_id="expiry-alerts"` and 422'd — for every caller, not just the demo token that surfaced it. `ComplianceHub.jsx`'s `ComplianceCalendar` polls this endpoint on load. Fix: swapped the two `include_router` calls so the static route registers first. Pinned by `test_fnd_051_evf_qco_expiry_alerts_route_shadowed.py`. | pinned |
| FND-052 | Dashboard operator persona's "Avg Score" KPI tile is a hardcoded fake value with no `placeholder` flag | build (STORY-413 Discover) | 2026-07 | `PERSONA_KPIS.operator` (`frontend/src/pages/Dashboard.jsx`) had a 4th tile, `{ label: "Avg Score", value: 41, ... }`, with no `placeholder: true` marker — but `deriveKpis` only ever overwrote index 0 for the operator persona; this tile was never touched by live data. It predated STORY-413 and wasn't among the 8 tiles that story's spec named, so STORY-413's build correctly left it alone rather than silently guessing at its intended semantics or shipping a fix without a spec. Owner decision: no real semantics were ever specified (average of what, over what window) and no backend endpoint exists — remove it outright, same treatment as the other 8 fake tiles ("don't want to show fake scores in demo"). Fix: deleted the tile object from `PERSONA_KPIS.operator`; `operator` now shows 1 persona tile (Scans Today) + the 2 universal live tiles (Audits (7d), Coverage %) = 3 visible tiles, the same count `ai_auditor` already renders — no new `KpiRow` layout case. Pinned by `frontend/src/pages/Dashboard.test.jsx`'s "FND-052" case — verified via fault injection (re-added the tile, confirmed the test failed red, removed it again, confirmed green). | pinned |
| FND-053 | `AppShell.navigateNow` followed off-`DEMO_TABS`-whitelist navigation for demo sessions | reviewer (STORY-412 round 1, BLOCKER) | 2026-07 | `Sidebar.jsx`'s `DEMO_TABS` filter only trimmed the Sidebar's own rendered button list — `AppShell`'s page router (`navigateNow`/`activePage`) had zero demo-mode awareness, so any in-page `onNavigate(page)` call (reproduced via `TraceView.jsx`'s always-visible "How SARO Reasons ↗" link → Trust Center → `GET /api/v1/governance/trust-documents`, gated `require_role("super_admin","operator")` with no persona fallback) reached a real 403, rendered as an empty "No trust documents configured" state — one click deeper than the Sidebar, but the exact failure mode the story exists to prevent. Fix: `navigateNow` (the single choke point all navigation flows through) now no-ops any target outside `DEMO_TABS` for `role === "demo_viewer"`; `TraceView.jsx` additionally hides the link entirely for demo sessions (defense in depth, not a substitute). Pinned by `frontend/src/components/AppShell.test.jsx`. | pinned |
| FND-054 | `ComplianceHub`'s readiness checklist checkbox rendered enabled for a read-only (demo) session | reviewer (STORY-412 round 1, BLOCKER) | 2026-07 | `services/readiness_service.py`'s `editable` flag has no `read_only`/role awareness, so `ComplianceHub.jsx`'s checkbox (`disabled={!item.editable || unknown}`) was clickable for a demo session. Clicking it PUT `/api/v1/compliance/readiness/{key}`, correctly 403'd server-side by `require_write_persona` (demo tokens are always `read_only=True`, `auth.py`), but the frontend's `.catch()` replaced the entire Readiness Checklist card with a `SectionError` — a disabled write path presented as available, and an empty-by-error screen. `ComplianceHub` also didn't even receive the `user` prop `AppShell` already passed it. Fix: added the `user` prop; checkbox `disabled` gained `\|\| !!user?.read_only`; `toggleReadiness` itself early-returns on `read_only` too. Pinned by `frontend/src/pages/ComplianceHub.test.jsx` ("FND-054 / STORY-412" case) — verified via fault injection (reverted the fix, confirmed the test fails red, restored, confirmed green). | pinned |
| FND-055 | `saro ingest --account-id`/`--region` silently override values derived from a resolved `TenantLogSourceConfig` | security-auditor (STORY-408) | 2026-07 | `cli.py`'s `ingest` command computes `effective_account_id = account_id or resolved_account_id` (and same for region) even when `--source` was omitted and a tenant's cross-account config was resolved. A mismatched operator-supplied account-id/region can't cross tenant/bucket boundaries (`allowed_s3_buckets` stays pinned to the resolved tenant's bucket), but it silently changes the `AWSLogs/{account_id}/.../{region}/` discovery prefix against that bucket — likely producing a confusing zero-results run rather than a clear error. Forward work: reject `--account-id`/`--region` when a tenant config is being resolved (or log a visible warning when the override diverges from the resolved value). No regression test yet — not a currently-wrong behavior, a UX/footgun hardening item. | open |
| FND-056 | `adapters/bedrock/cross_account.py`'s `assume_role_credentials`/`refreshable_session` accept `role_arn`/`external_id` as independent parameters — no structural guarantee against a future mismatched-tenant call | security-auditor (STORY-408) | 2026-07 | The only production caller (`S3LogStore.for_tenant`) sources both values from the same `ValidatedSourceConfig`, so there is no current confused-deputy path — this is caller-discipline-enforced, not structurally enforced. Forward work: fold `role_arn`+`external_id` into a single credential-params object so a future caller cannot independently vary one without the other. No regression test yet — tech debt / defense-in-depth hardening, not a currently-exploitable gap. | open |
| FND-057 | `scripts/seed_demo_tenant.py`'s `get_demo_jwt()` POSTed a form-encoded body to a JSON-only endpoint — every demo-tenant login attempt 422'd | reviewer (PR #119 demo-login fix) | 2026-07 | `get_demo_jwt()` sent `data={"username": ..., "password": ...}` (form-encoded) to `/api/v1/auth/token`, but that route's `LoginIn` (`schemas.py`) is a plain Pydantic body with no `Form()` dependency — FastAPI parses it as JSON only, so the form-encoded POST never satisfied it. The demo tenant (`e84e6a6c-e774-4e34-83cb-8610631b09dd`) had ingested findings but no way to obtain a working login. Fix: send `json={"email": ..., "password": ...}` matching `LoginIn.email`/`LoginIn.password`. Pinned by `test_fnd_057_seed_demo_jwt_json_body.py`. | pinned |
| FND-058 | `ensure_demo_user()` silently reassigned tenant_id/role/password on any user matched by email | security-auditor (PR #119 review, `fix/seed-demo-tenant-login`) | 2026-07 | `scripts/seed_demo_tenant.py`'s `ensure_demo_user()` looked up an existing user by `SELECT id FROM users WHERE email = :email` only, then unconditionally reassigned `tenant_id`, escalated `role` to `'super_admin'`, and reset the password on any match — no check that the matched row already belonged to the demo tenant or already had `role='super_admin'`. `email` is globally unique (`models.User.email`), so low-likelihood in practice (would require the demo email to be reused by an unrelated real account), and the caller already needs `DATABASE_URL` access (highest privilege) to run this script — non-blocking hardening item. Fix: before overwriting, if the matched row's `tenant_id` is non-null and either differs from the target tenant or its role isn't already `'super_admin'`, raise instead of silently reassigning it (UUID comparison normalized via `uuid.UUID(...)` since raw-SQL `SELECT` bypasses the ORM's UUID type decorator). Pinned by `test_fnd_058_ensure_demo_user_tenant_guard.py` (+ unit-level guard cases in `tests/test_s000_seed.py::TestEnsureDemoUser`). | pinned |
| FND-059 | `scripts/seed_demo.py` stored `overall_risk_score` pre-multiplied by 100 — seeded demo tenants rendered "1286/100" risk chips | demo readiness review 2026-07-19 (F1, PR #123) | 2026-07 | Every production writer (`routers/scan.py`, `routers/ingest.py`, `routers/output_audit.py`, `routers/hf_processor.py`, `services/audit_submission.py`) persists the engine's 0–1 probability (`bayesian_scores.overall`, rounded 4 dp in `engine.py`), and the frontend scales ×100 once for display (`TraceView.jsx`, `ComplianceHub.jsx`). `seed_demo.py` was the lone outlier writing `overall * 100`, so only seeded demo tenants double-scaled. Fix: store the unscaled probability (`round(overall, 4)`), matching the production writers; demo tenants seeded with the buggy script need a reseed. Pinned by `test_fnd_059_seed_demo_risk_score_scale.py`. | pinned |
| FND-060 | Deployed sidebar permanently showed a red "API offline" badge — `/health` never reached the backend and lacked the `db_ok` field | demo readiness review 2026-07-19 (F2, PR #123) | 2026-07 | Three legs: (1) `frontend/nginx.conf` proxied only `location /api/`, so `Sidebar.jsx`'s `fetch("/health")` fell through to the SPA fallback and got index.html (200, HTML) — `r.json()` threw and health stayed null; (2) the Vite dev proxy had the same gap; (3) `/health`'s payload carried `database: "ok"` but not the `db_ok` boolean Sidebar reads (and CLAUDE.md/`docs/ARCHITECTURE.md` document). Fix: `location = /health` proxy in nginx.conf, `/health` entry in the Vite dev proxy, and an additive `db_ok` boolean in the payload (`database` string kept for existing consumers). Pinned by `test_fnd_060_frontend_health_proxy.py`. | pinned |
| FND-061 | Jira OAuth callback accepted an unsigned optional `state` as the tenant id — no CSRF/state integrity on token binding | threat model TM-F1 (`docs/security/threat-model.md`, STORY-365 — lands with `story/epics-14-19-pack`; its TM-F1 disposition row needs a "Fixed — FND-061" update when that branch merges) | 2026-07 | `GET /api/v1/remediation/oauth/jira/callback` (`routers/remediation.py`) parsed any caller-supplied `state` string as a tenant UUID and bound the exchanged Jira tokens to that tenant — unauthenticated, unsigned, no expiry, no one-time use, so an attacker could complete a forged callback against a victim tenant (classic OAuth CSRF). Fix: `/oauth/jira/start` now issues `"{tenant_id}.{ts}.{nonce}.{hmac_sha256}"` (key = `OAUTH_STATE_SECRET` env var falling back to `settings.jwt_secret_key`, **no hardcoded default** — FND-044 class deliberately avoided) and persists the nonce on `Tenant.settings_json`; the callback rejects missing/tampered/forged/expired (10-min TTL)/replayed state with 400 **before** any code-for-token exchange, and consumes the nonce at verification (strict single-use). Pinned by `test_fnd_061_jira_oauth_signed_state.py` (all six rejection/acceptance paths + zero-egress assertions). | pinned |
| FND-062 | Jira OAuth tokens written to a nonexistent `ClientConfig.settings_json` attribute — silently never persisted | build (fixing FND-061/TM-F1) | 2026-07 | `models.ClientConfig` has no `settings_json` column (only `Tenant` does), so the callback's token write raised `AttributeError`, was swallowed by its broad `except` ("Failed to store Jira tokens" warning), and returned 200 anyway — tokens never persisted; `create_jira_issue`'s matching read (`config.settings_json`) meant the Jira integration could never actually work (400 "not configured" when no ClientConfig row, 500 AttributeError when one existed). Fix: both the callback write and the `create_jira_issue` read moved to `Tenant.settings_json` (existing column, row always exists — no migration). Pinned by `test_fnd_061_jira_oauth_signed_state.py`'s happy-path round-trip (encrypt → persist on Tenant → decrypt). | pinned |
| FND-063 | `GET /remediation/oauth/jira/start` has no role/read-only gate — any authenticated tenant user can rebind the tenant's Jira connection | security-auditor (FND-061 review, pre-existing) | 2026-07 | The route depends on bare `get_current_user` only, so any authenticated user in the tenant (including read-only personas) can initiate a new OAuth flow and rebind the whole tenant's Jira integration to their own Atlassian account — an insider-integrity issue, not cross-tenant. Also: the pending state nonce is a single per-tenant slot on `Tenant.settings_json`, so a second in-flight flow clobbers the first. Forward work: restrict `/start` (and thereby rebinding) to an admin/owner role; consider keying pending state by nonce to allow concurrent flows. Pre-dates FND-061's diff. No regression test yet. | open |

| FND-064 | Incident Response Plan commits to detection and response times the current alerting cannot deliver — and it is customer-facing | build (STORY-369 SLO/SLA premise check) | 2026-07 | `docs/incident-response-plan.md` v1.0 states "Detect (0–5 min): Automated alert fires" (§2), "System Downtime (P1) \| Automated (<5 min) \| 15 min acknowledgement" (§8), and P1 "1-hour response, 2-hour restore" (§6). Measured reality after STORY-368: the canary probes every 30 minutes and alert A1 requires **two consecutive** failures, so detection latency is **≤60 min, not <5 min**; the delivery channel is GitHub-Actions-failure → operator email, explicitly **not a pager** (`docs/ops/alerts.md` §3), so a 15-minute overnight acknowledgement is not supportable. The §7 escalation matrix also names an "On-Call Engineer" reachable at "Slack @oncall" — there is no on-call rotation and no Slack alerting integration; SARO is a solo-operator model. §2 detection triggers still reference "Railway health check failure" although Railway is SUPERSEDED by Fly.io (`docs/ARCHITECTURE.md`). **Exposure:** this is not an internal doc — `GET /api/v1/governance/ir-plan` (`routers/governance.py:77`) serves `sla_hours: 1` for downtime and data breach to the frontend Trust Center, and the full document is downloadable via `/governance/docs/ir-plan`; GAP_ANALYSIS_2026-06-15 records this plan as having closed external-sharing gap #1. So these are commitments a customer can already read. Deliberately **not** silently rewritten here: changing published commitments is an owner decision, and STORY-371 owns the IRP delta. `docs/legal/sla-draft-v0.1.md` §5 therefore points at the IRP rather than restating response targets, and is blocked from external use until this is reconciled. Forward work (STORY-371): either reduce the committed times to what the email channel supports, or add a paging tier and keep them; correct the escalation matrix to the actual solo-operator model; drop the Railway references. **Fixed (STORY-371):** IRP bumped to v1.1 with a changelog explaining the correction; severity/response targets moved to the single source `docs/ops/support-model.md` §3 (targets honest for a solo operator on email: S1 = 1 business hour in hours, best-effort outside); detection stated as ≤60 min matching the canary; **notification commitments separated from response** (a 72-hour breach-notification clock IS achievable where a 15-minute response clock was not); escalation matrix corrected to the actual solo-operator model with the missing backup responder flagged `[HUMAN — OPEN]`; Railway replaced with Fly.io; `routers/governance.py` `sla_hours` updated to match the doc. Pinned by `test_fnd_064_ir_plan_response_commitments.py` (asserts doc↔endpoint↔support-model agree and that no undeliverable target returns). | pinned |
| FND-065 | Nothing records authentication events — a leaked credential's usage cannot be reconstructed | STORY-371 tabletop exercise (2026-07-21) | 2026-07 | `services/self_audit.py` defines `AUTH_EVENT` in its closed action-class vocabulary, but **no code path in the product emits it**: `routers/auth.py` calls `record_privileged` only for tenant provisioning and persona changes, and the login/register/bootstrap routes write nothing. So SARO cannot answer "was this credential used, and by whom?" — the question that determines whether a credential exposure is a reportable incident. Surfaced by walking the FND-003 scenario end to end. **Also a self-inflicted documentation error:** `services/admin_audit_registry.py` classified `POST /api/v1/auth/token` as DATA_PLANE with the justification "login — auth events handled by the auth path", asserting a mechanism that does not exist (CLAUDE.md FM-2, unverified premise). Justification corrected to state the truth. **Fixed:** `routers/auth.py` now emits `AUTH_EVENT` on **both** login success and login failure (the failure half is what makes credential stuffing and brute force visible) with actor, outcome, and `source_ip`. Attempts against an unknown email have no tenant, so they land on `SYSTEM_TENANT_ID` rather than being dropped for want of a chain. The emitter **fails open**: a self-audit write failure logs at ERROR and lets the login proceed, because fail-closed would lock out every user *including the operator trying to diagnose the database problem causing it* — matching the availability tradeoff already documented in `services/self_audit.py`. Metadata only; an AST-level test asserts the emitter's executable statements never touch password or token material. The query half already existed (`GET /api/v1/audit/events` filters by `actor` + `action_class`), so one principal's session history is now reconstructable. `POST /api/v1/auth/token` reclassified DATA_PLANE → **AUDITED**. Logout is out of scope — no logout endpoint exists (FND-002, open). `/auth/register` and `/auth/bootstrap` create users rather than sessions; their coverage is still outstanding and is stated as such rather than assumed. Pinned by `test_fnd_065_auth_event_coverage.py` (10 tests, red-first: 7 failed before the fix). | pinned |
| FND-066 | Password rotation does not invalidate outstanding JWTs — no session revocation exists | STORY-371 tabletop exercise (2026-07-21) | 2026-07 | `auth.py` and `models.py` contain no `jti`, no `token_version`, and no denylist/blacklist; tokens are validated on signature + expiry alone. Consequence: the containment step in `docs/security/secrets-runbook.md` §3 ("rotate the credential, verify the old one is dead") is **incomplete** — an attacker holding a token minted before rotation keeps full access until that token expires, and rotation gives false confidence that access was cut. Directly weakens the FND-003 remediation. **Fixed:** `users.token_version` added (migration 038, `server_default 1`); `auth.create_access_token` embeds it; `auth.get_current_user` rejects any mismatch — **including a missing claim**, so tokens issued before this shipped fail closed rather than being trusted (everyone re-authenticates once at deploy, the correct price for not leaving the hole open for every issued token's lifetime), and **including a higher-than-expected version**, since that is forged or replayed. `auth.revoke_user_sessions(user)` bumps the generation without committing, so revocation lands atomically with the change that prompted it. There is currently no change-password endpoint; the only existing reset path (`scripts/seed_demo_tenant.py`'s raw SQL) now bumps the column too. Chose a version integer over a `jti` denylist: one column compared against an already-decoded claim, revoking all of a user's sessions at once — which is what credential rotation needs — versus storage growing per revocation plus a lookup on every request. Pinned by `test_fnd_066_token_revocation.py` (10 tests, red-first: 6 failed before the fix), including a guard that fails the build if a future change-password route appears without revocation wiring. | pinned |
| FND-067 | No breach-notification template and no tenant security-contact list | STORY-371 tabletop exercise (2026-07-21) | 2026-07 | `docs/ops/support-model.md` §4 commits to notifying an affected customer within 72 hours of confirming a reportable security incident, and the IRP references regulatory notification, but there is no drafted notification template and no maintained per-tenant security-contact list — the `tenants` table has no security-contact field. Drafting customer-facing breach wording under time pressure is when it goes wrong, and locating contacts mid-incident wastes the clock. Forward work: commit a notification template (initial + update + closure) to `docs/ops/`, add a security-contact field to tenant onboarding (STORY-373), and have counsel review the template before it is ever needed. No regression test yet. | open |

| FND-068 | RP-OBS-COMPLETE's required-fields check missed an EMPTY required field, only a null one | STORY-378 confusion-matrix harness (first run) | 2026-07 | `rule_packs/observation/evaluate.py::_check_required_fields` tested `getattr(record, name) is None`. But the contract's required string fields (`model_id`, `operation`) are never None — an adapter that cannot resolve one emits `""` and marks `field_availability` MISSING (e.g. an endpoint-deployed Vertex model, STORY-360). So OBS-REQUIRED-FIELDS-1 could NEVER fire on the exact real-world completeness gap it exists for — an evidence rule silently blind to a whole class of missing data. Surfaced the moment the STORY-378 harness measured the pack against labeled ground truth: recall on OBS-REQUIRED-FIELDS-1 was 0. Fix: treat a field as absent when it is None OR `== ""`; a numeric 0 stays present (a real count is not an empty value). Red-first: 2 of 5 tests failed before the fix; harness recall went 0.857 → 1.0 after. Pinned by `test_fnd_068_empty_required_field.py`. | pinned |
| FND-069 | Route-registration tests filtered `app.routes` at the top level; green locally but red in CI because an unpinned Starlette nests included routers | CI red on `main` (Quality Gates coverage run), surfaced once the story-index shallow-clone failure was un-masked (2026-07-22) | 2026-07 | `test_story365/366/368/382` enumerated routes with `[r for r in app.routes if isinstance(r, APIRoute)]`. Starlette varies by version: some FLATTEN included-router routes into `app.routes` as `APIRoute`s, others NEST them inside a path-less sub-router object. `requirements.txt` pins only `fastapi>=0.111.0`, so local (fastapi 0.129.0 / starlette 0.52.1 — flattening) saw all 204 routes, while CI's fresh install (nesting) saw only the two top-level `@app.get` routes (`/`, `/health`) — reporting the fully-wired app as a **2-route app** and failing all four route-registration assertions. The routes were always registered; only the test enumeration was wrong (not a product regression, not test pollution). Fix: shared `tests/_route_utils.iter_api_routes()` recurses through nested sub-routers (version-independent); the four tests now use it. `test_sar010_controls` had already dodged this via `app.openapi()`, but that omits `include_in_schema=False` routes like `/metrics`, so recursion over `app.routes` is the general fix. Red-first: the regression's `test_naive_top_level_filter_misses_nested_routes` demonstrates the miss under forced nesting. **Primary fix applied:** pin `fastapi==0.129.0` + `starlette==0.52.1` in requirements.txt so CI installs the validated-local (flattening) versions — the recursive helper alone was insufficient because CI's Starlette nests in a shape the `.routes` recursion did not traverse (proved when `test_helper_works_on_the_real_app` returned 2 routes in CI). The helper + regression remain as defensive robustness. Pinned by `test_fnd_069_route_enumeration_nesting.py`. | pinned |
| FND-070 | Validation report "Generated:" date stamped `datetime.now()` while STORY-379 AC-1 pins the committed report byte-equal to `build_markdown()` — a time-bomb that turned the suite red the day after every regeneration | Stop-hook full-suite run during demo E2E work (2026-07-22) | 2026-07 | `scripts/generate_validation_report.py` rendered `datetime.now(tz=timezone.utc).date()` into the report header, so `test_report_is_current_with_the_artifact` (byte-equality of committed md vs fresh `build_markdown()`) could only pass on the exact day the report was last regenerated; it was committed 2026-07-22 and failed 2026-07-23. Fix: the date derives from the artifact's own `generated_at` (`_artifact_date()`), honoring the story's principle that every fact in the report comes from the artifact — `build_markdown()` is now deterministic. Regenerated report is byte-identical to the committed one. Pinned by `test_fnd_070_validation_report_date_from_artifact.py` (frozen artifact date must render; wall-clock date must not leak). | pinned |
| FND-071 | `confusion_matrix_harness.py --check` rewrote `quality/validation/confusion-latest.json` and appended `trend.jsonl` on every run — pytest and CI mutated the checked-in validation artifacts | Same stop-hook investigation (2026-07-23): artifact `generated_at` jumped mid-suite; 7 identical duplicate trend lines had accumulated | 2026-07 | `main()` wrote the matrix artifact and appended a trend line unconditionally; `test_check_mode_passes_because_t1_meets_the_signed_bar` calls `main(["--check"])`, so every full pytest run dirtied the tree (fresh `generated_at` + duplicate trend line) and re-broke STORY-379's committed-report equality even after FND-070. Fix: `--check` is now a read-only verification mode (compute + verdict + exit code, no writes); only default-mode runs persist the artifact of record and its trend line. CI (`conformance.yml`) only consumes the exit code and its job-summary `cat` now shows the committed artifact. Duplicate trend churn restored to HEAD. Pinned by `test_fnd_071_check_mode_is_read_only.py` (byte-snapshot both files across `main(["--check"])`). | pinned |
| FND-072 | `confusion_matrix_harness.py --check` validated nothing about the committed evidence artifact — a rule-pack precision/recall regression left `quality/validation/confusion-latest.json` silently stale in CI | Security-audit follow-up on PR #125 (2026-07-23): reviewing FND-071's blast radius showed the removed write had been an accidental freshness gate | 2026-07 | 5-whys: CI could ship stale evidence ← `conformance.yml`'s validation-report step compares two *committed* files (report vs artifact), a tautology ← the artifact used to be rewritten in the CI workspace by `--check`, so that comparison implicitly checked committed-vs-current ← FND-071 removed the write (correctly — it dirtied every pytest/CI tree) without replacing the freshness property it accidentally provided ← the freshness gate was never an explicit, named control, so nothing failed when it vanished. Fix: in `--check` mode the harness diffs the freshly computed matrix against the committed `MATRIX_OUT` excluding `generated_at` (the only field legitimately allowed to differ — same exclusion as the STORY-378 determinism test) and exits non-zero on mismatch, missing, or corrupt artifact, printing the regenerate command. Still zero writes on every `--check` path (FND-071 pin stays green). Pinned by `test_fnd_072_check_mode_detects_stale_artifact.py` (tampered/missing/corrupt copy via monkeypatched `MATRIX_OUT` must fail; timestamp-only delta must pass; failure path must not write). | pinned |
| FND-073 | Remediation tab structurally dead — read a nonexistent response key (permanent "all clear") and PATCHed a nonexistent endpoint | UI review of sarofrontend.fly.dev /app tabs (2026-07-23, STORY-TAB-001) | 2026-07 | `Remediation.jsx` read `d.items` from `GET /api/v1/remediation`, but the route returns `{traces, total, page, page_size, pages}` (routers/remediation.py:583) — so the tab rendered "✓ No open remediation actions — all findings have been addressed" unconditionally, an implicit all-clear overclaim on the human-in-the-loop work queue while open fail/warn traces existed. Its "Mark Complete" button PATCHed `/api/v1/remediation/{id}/complete`, a route that has never existed (canonical: PATCH `/api/v1/remediation/traces/{id}/remediate`, which also *requires* a non-blank `remediation_note` — the old UI collected none, so even with the right path every call would have 422'd); the swallowing `catch {}` made the button appear to succeed. Card fields (`severity`/`title`/`description`/`guidance`) also matched nothing the API returns. Fix (frontend-only; API treated as canonical): list reads `traces`/`total`; cards render the real fields (`check_name`, `reason`, `remediation_hint`, `result` badge, `effort_estimate`, `regulation_ref`); Mark Complete opens a required-note input and PATCHes the canonical route with `{remediation_note}` (no request while blank); PATCH failure surfaces an error and keeps the card; "Showing N of M" when `total` exceeds the page. Pinned by `frontend/src/pages/Remediation.test.jsx` (red-first: 8 of 10 failed pre-fix). | pinned |
| FND-074 | No CI workflow runs vitest — every frontend-pinned FND is enforced only on developer machines | reviewer (STORY-TAB-001, pre-existing) | 2026-07 | `.github/workflows/deploy.yml`'s `frontend` job runs only `npm ci` + `npm run build`; `ci.yml` runs pytest only. So the ~10 manifest entries pinned by `frontend/src/**/*.test.jsx` (FND-021/022/023/029/031/052/053/054/073…) have no CI guarantee — a revert of any of them (e.g. Remediation back to `d.items`) would merge green. Repo enforcement model says "CI guarantees"; for the frontend tier it currently doesn't. Forward work: add a vitest step (`npx vitest run`) to the frontend job (or a dedicated frontend-test job in ci.yml) before build, and consider wiring `npm run lint`. No regression test yet (the pin for this would be CI config itself). | open |
| FND-075 | Onboarding progress fabricated on two surfaces — tab and first-login wizard both hardcoded 7 step ids the API never returns → permanent 0% | UI review of /app tabs (2026-07-23, STORY-TAB-002) | 2026-07 | `GET /api/v1/onboarding/status` returns `{steps:[{key,label,completed,cta_url}], completed_steps, total_steps, completion_pct, onboarding_complete}` with 4 steps (routers/onboarding.py:68-105), but both consumers — `pages/Onboarding.jsx` and `AppShell.jsx`'s `OnboardingWizard` (shown to every admin on first login) — hardcoded 7 divergent step ids (`tenant_created`, `users_invited`, …) and read flat booleans (`progress[s.id]`), so both rendered 0% with permanently-unchecked steps regardless of the tenant's real state; the tab also faked an empty-object success on fetch failure (`r.ok ? r.json() : {}`), and the wizard's Go buttons were keyed to the phantom ids. Fix: both surfaces render the API `steps` verbatim (labels, `completed`, API order — data-driven so future keys render without frontend changes); progress derives only from `completion_pct`/`completed_steps`/`total_steps`; shared `config/onboardingNav.js` maps step keys with an unambiguous in-app destination (`first_scan`→upload, `aims_doc`→aims) to Go buttons — API `cta_url` values are API paths and are never rendered as links; fetch failure shows an explicit error (tab) / neutral no-progress line (wizard), never a fake 0%. Pinned red-first by `frontend/src/pages/Onboarding.test.jsx` + `frontend/src/components/AppShell.test.jsx` (11 of 14 failed pre-fix). | pinned |
| FND-076 | Coverage Gap rendered blank framework names and "No gaps identified for undefined" — every detail field it read was phantom | UI review of /app tabs (2026-07-23, STORY-TAB-003) | 2026-07 | `GET /api/v1/compliance-matrix/coverage` rows carry `framework/total_rules/covered/partial/not_covered/coverage_pct/last_updated` (routers/compliance_matrix.py:174-207), but `CoverageGap.jsx` read `fw.name` (list label AND React key → blank names, duplicate undefined keys), `fw.gaps`/`fw.gaps_count`/`fw.controls_covered`/`fw.controls_total` (never present), so the detail pane's only reachable state was the green "✓ No gaps identified for undefined" — a positive no-gap claim built from absent data, shown even for the live tenant's 0%-coverage frameworks. Fix: list/detail/selection key off `framework`; detail renders the API's own covered/partial/not_covered counts against `total_rules` ("N of M rules not covered" as the gap signal; positive all-covered state only when not_covered=0 AND partial=0); overall summary row surfaces `overall_coverage_pct`/`framework_count`/`total_rules`; explicit no-data state for an empty `frameworks` array. No fabricated per-control gap list (per-rule drill-down needs a new endpoint — story Out of Scope). Pinned red-first by `frontend/src/pages/CoverageGap.test.jsx` (6/7 failed pre-fix). | pinned |
| FND-077 | Evaluations tab dead-on-arrival for the personas the nav showed it to — backend gate excluded `admin`, trigger button keyed to personas the backend rejects | UI review of /app tabs (2026-07-23, STORY-TAB-004) | 2026-07 | `GET /api/v1/evaluations` was `require_role("super_admin","operator")` while `Sidebar.jsx` surfaced the tab to the `compliance_lead` and `admin` personas, so an admin opening their own tab rendered a bare "⚠ 403"; `Evaluations.jsx` additionally keyed its "+ Trigger Eval Run" button off `persona_role ∈ {ai_auditor, admin, super_admin}` although `POST /trigger` is `require_role("super_admin")` on the ACCOUNT role — the button was shown to users whose trigger would always 403 and (via persona-switching) hidden from super_admins it would serve. Owner decision (least privilege): `admin` added to the LIST gate only (read-only QA metadata; one-word change); trigger gate untouched; `evaluations` removed from the `compliance_lead` persona tab list; trigger button keyed off `user.role === "super_admin"`; list 403 renders an access explanation instead of the bare code. Pinned red-first (admin-list case failed pre-fix) by `tests/regression/test_fnd_077_evaluations_list_rbac.py` + `tests/test_tab004_evaluations_rbac.py` (role matrix) + `frontend/src/pages/Evaluations.test.jsx` / `Sidebar.test.jsx` (nav + button halves). | pinned |
| FND-078 | Evaluations list response exposes `api_url` (internal infra endpoint) and raw `error_message` exception text to every listing role | security-auditor (STORY-TAB-004, pre-existing — audience widened by FND-077's fix) | 2026-07 | `EvaluationRunOut` (routers/evaluations.py) serializes `api_url` (raw `SARO_API_URL`) and `error_message` (`str(exc)[:2000]` — arbitrary internal exception text that can carry paths/URLs from TestRunner failures) on the LIST endpoint. No credentials (SARO_INTERNAL_TOKEN/HF_TOKEN are never persisted to the row), and the audience is privileged roles only — but the fields are internal-infrastructure detail no list consumer needs. Forward work: drop `api_url`/`error_message` from the list serialization (keep on `EvaluationRunDetailOut`, which stays super_admin/operator), or sanitize `error_message` to a fixed enum + reference id. No regression test yet. | open |
| FND-079 | `require_role`'s 403 detail echoes the required role tuple to the client, unlike `require_role_or_persona`'s deliberately generic denial | security-auditor (STORY-TAB-004, pre-existing) | 2026-07 | auth.py:303-304 returns `"Role '{role}' is not authorised… Required: {roles}"` while `require_role_or_persona` (auth.py:348+) intentionally does NOT echo role/persona identifiers (FND-036 hardening). Inconsistent information-disclosure posture across the two authz helpers; low severity (role names are not secrets) but the asymmetry invites copy-paste of the chattier variant. Forward work: harmonize `require_role` to the generic denial; when done, also generalize the role list enumerated in `Evaluations.jsx`'s 403 message. No regression test yet. | open |
| FND-080 | Drift Alerts version grid never rendered (phantom `framework_versions` key) and alert cards referenced fields that have never existed | UI review of /app tabs (2026-07-23, STORY-TAB-005) | 2026-07 | `GET /api/v1/rules/drift-alerts` returns `current_versions`/`latest_known_versions` (dicts) + `checked_at` (routers/rule_packs.py:62-68), but `DriftAlerts.jsx` read `data.framework_versions` (array) — permanently `[]`, so the "Current Framework Versions" grid was unreachable dead code; alert cards also rendered `what_changed`/`affected_rule_packs` sections for fields `check_drift` has never produced (services/rule_service.py:59-72), and the green "all framework versions are current" state carried no freshness evidence. Fix: grid merges tracked ∪ packed frameworks (a tracked framework with no active pack now shows "no pack" — the absence is itself signal); alert cards render `message` + framework/current→latest only; the no-drift state carries `checked_at` ("Last checked …") so the reassurance is evidently a fresh computation (FND-030 anti-overclaim class); explicit neutral state for empty version maps. Pinned red-first by `frontend/src/pages/DriftAlerts.test.jsx` (6/7 failed pre-fix). | pinned |

**`verify-pinned`** = fix believed shipped, but no regression test confirms it stays fixed.
First task of any auth story: convert these to `pinned` by writing the tests.

**Cross-file status mapping (ledger ↔ `tests/regression/manifest.yaml`):** a finding that is `pinned` here appears as `status: pinned` in the manifest; a finding that is `verify-pinned` or `open` here (no regression test yet) appears as `status: open` in the manifest. The two records track the same FND IDs and must never contradict each other — `tests/test_story103_findings_ledger_consistency.py` enforces this.
