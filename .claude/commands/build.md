---
description: Run any task (non-story work) through the full SARO lifecycle
argument-hint: <task description, however rough>
---

Task: $ARGUMENTS

Run this through the saro-lifecycle skill end to end:

1. Classify trivial vs. standard; create implementation-notes.md from the
   lifecycle template.
2. Walk DISCOVER → SHAPE → PREVIEW → PLAN → BUILD → VERIFY, applying each
   stage's trigger/skip rules. One-line announcement per stage transition,
   then execute. Ask before skipping any stage whose trigger fired; never
   ask permission to run one.
3. If my task description is rough (a problem, not a change), Stage 1a
   brainstorm is mandatory before anything else.
4. SELL only if I ask.
